This directory contains the original MATLAB reference implementation of the
Ghil-Sellers Energy Balance Model.

The files are kept with their original names so the Python port can be
checked against the source implementation during the migration.
